#include<stdio.h>
int main(){
  int x = 0;
  int *p = &x; // gán địa chỉ x vào con trỏ p

  //xin địa vùng nhớ - cấp phát
  // void * malloc (size)
  float * fp = (float *) malloc(10 * size of(float));
  // calock // xoá
  //realloc // điều kiển
}
